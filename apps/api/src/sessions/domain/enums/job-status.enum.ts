export enum JobStatus {
  pending_enqueue = 'pending_enqueue',
  enqueue_failed = 'enqueue_failed',
  queued = 'queued',
  running = 'running',
  done = 'done',
  failed = 'failed',
  paused = 'paused',
}
